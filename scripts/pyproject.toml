[project]
name = "scripts"
version = "0.1.0"
description = "DevOps utility scripts for infrastructure management"
requires-python = ">=3.14"
dependencies = [
    "boto3>=1.42.53",
    "botocore[crt]>=1.42.53",
]
